# The N Resort Management — 배운 교훈

> 진행하며 마주친 함정, 실수, 발견, 의사결정 이유를 기록. 다음 프로젝트/세션에서 같은 실수를 피하기 위함.

각 항목 구조:
- **날짜**: YYYY-MM-DD
- **상황**: 무엇을 하고 있었나
- **발견/문제**: 무엇이 잘못됐거나 놀라웠나
- **배운 점**: 원인·교훈
- **앞으로의 적용**: 이후 어떻게 다르게 할지

---

## L-001. "완전 단절"의 범위를 좁게 정의해야 한다
- **날짜**: 2026-09-03
- **상황**: 1차 플랜의 "원작자 리소스와 완전한 단절" 실행 중.
- **발견/문제**: "완전 단절"을 넓게 해석해 원작자의 코드/스키마/문서를 통째로 삭제(`supabase/migrations/` 26개, edge functions, `conductor/`, `docs/*` 등). 사용자가 "이 프로그램 형식을 내가 이용할 거니까 코드까지 지우지 마라"고 정정. dangling commit(`c145e1a`)에서 파일 복원.
- **배운 점**: "단절"이라는 단어는 사용자와 나 사이에 스코프가 다를 수 있다. 사용자는 "원작자 계정·리소스 연결"만 의미, 나는 "원작자와 관련된 모든 것"으로 해석. 삭제 전에 반드시 범위 목록을 사용자에게 확인받아야 한다.
- **앞으로의 적용**: 광범위 삭제 작업 전에는 항상 삭제 대상 목록(구체 파일명)을 표로 정리해서 사용자 승인. "다 지운다" 같은 표현이 나와도 범위 재확인.

---

## L-002. GitHub Push Protection이 시크릿을 잡아내면 우회하지 말고 정면 해결
- **날짜**: 2026-09-03
- **상황**: 새 저장소로 첫 push.
- **발견/문제**: 원작자의 Resend API 키(`re_KZvnbYHQ...`)가 3개 SQL 마이그레이션 파일에 하드코딩되어 있어 push 거부됨. GitHub이 unblock URL을 제공하지만 그건 실제 시크릿을 그대로 노출시키는 것.
- **배운 점**: GitHub의 secret scanning + push protection은 정확하다. unblock 옵션은 절대 쓰지 말고 시크릿을 제거 or 플레이스홀더로 치환한 뒤 amend + push해야 한다.
- **앞으로의 적용**: 첫 push 전에 로컬에서 grep으로 알려진 시크릿 패턴(`re_`, `AIza`, `sk-`, `eyJ...`)을 미리 스캔. 특히 원작자 코드를 인수한 경우 SQL/설정 파일 우선 점검.

---

## L-003. 대화창에 실제 API 키를 붙여넣는 것은 위험하다
- **날짜**: 2026-09-03
- **상황**: 사용자가 자신의 Resend/Vercel 토큰을 채팅에 붙여넣음.
- **발견/문제**: 대화 로그에 실제 시크릿이 영구히 남게 됨. 세션 로그 유출 시 노출 위험.
- **배운 점**: 시크릿을 다룰 때는 대화창을 우회하고, 사용자가 직접 `.env`나 Supabase Dashboard 등 안전한 위치에 입력하도록 유도해야 한다.
- **앞으로의 적용**: 시크릿 값 자체는 절대 채팅에 요청하지 않는다. 안전한 위치를 명시해서 사용자가 직접 입력하게. 부득이 노출 시 즉시 rotate 안내.

---

## L-004. 로컬 nested git repo를 참조 폴더로 두면 실수의 여지가 있다
- **날짜**: 2026-09-03
- **상황**: `reference/project-management-app/`에 원작자 저장소가 자체 `.git`을 가진 채로 있었음.
- **발견/문제**: 상위 리포와 nested 관계인데 submodule도 아니라 관리 복잡. 결국 삭제.
- **배운 점**: 참고용 코드 사본은 프로젝트 밖 별도 위치에 두는 게 안전. 프로젝트 안에 두려면 최소한 상위 `.gitignore`에 명시.
- **앞으로의 적용**: 참고용 원본은 `~/references/` 같은 완전 별도 위치에 클론.

---

## L-005. 삭제 후 복원은 git 오브젝트 DB로 가능하다 (아직 GC되지 않았다면)
- **날짜**: 2026-09-03
- **상황**: 위 L-001 상황에서 원본 파일 복원 필요.
- **발견/문제**: `git branch -D main`으로 이전 브랜치를 지운 후에도 커밋 오브젝트(`c145e1a`)는 dangling 상태로 남아있음. `git reflog`와 `git fsck --lost-found`로 찾을 수 있음. `git checkout <commit> -- <path>` 로 파일 복원 가능.
- **배운 점**: git은 웬만해선 데이터를 즉시 지우지 않는다. 실수했을 때 당황하지 말고 reflog/fsck부터 확인.
- **앞으로의 적용**: 브랜치 삭제/hard reset 실수 시 반드시 GC 전(기본 90일)에 reflog 확인. 절대 `git gc --prune=now` 같은 즉시 GC 명령 남발하지 않기.

---

## L-006. 플랜 파일 이름은 실행 순서와 일치시켜야 한다
- **날짜**: 2026-09-03
- **상황**: 2차 플랜을 Phase A/B/C/D로 명명. A=삭제, B=스키마, C=코드, D=문서. 그런데 실행 순서는 D→A→B→C.
- **발견/문제**: 알파벳 순서와 실행 순서 불일치. 사용자가 "왜 D야?"라고 지적.
- **배운 점**: 순서가 있는 단계에는 알파벳 대신 숫자(Step 1, 2, ...)를 쓰거나, 알파벳을 쓰더라도 실행 순서에 맞춰야 한다.
- **앞으로의 적용**: 2차 플랜부터 **Step 1~4**로 통일. Step 1=문서, Step 2=삭제, Step 3=스키마, Step 4=코드.

---

## L-007. 사용자의 "실행 신호 대기" 요청은 명시적일 때 확실히 지킨다
- **날짜**: 2026-09-03
- **상황**: 사용자가 "내가 하라고 할 때 하는 거야"라고 명시.
- **발견/문제**: 이후 대화 흐름에서 "이거 좋아", "그렇게 하자" 같은 승인성 표현이 나와도 실행 시점은 별도라는 원칙.
- **배운 점**: 정보성 답변과 실행 승인은 다르다. 실행은 반드시 "실행해라", "진행해라" 같은 명시적 신호에만 응답.
- **앞으로의 적용**: 준비만 하고 "실행해도 될까요?"로 명시적 확인. 애매하면 실행하지 말고 물어보기.

---

## L-008. Gemini/AI 기능 폐기 결정은 초기에 알수록 좋다
- **날짜**: 2026-09-03
- **상황**: 원본 R&D 앱은 Gemini 기반 회의록 자동 생성이 핵심 기능. 초반 구조 파악 시 "AI 파이프라인은 재사용 가치 높음"으로 판정.
- **발견/문제**: 사용자가 "gemini는 안 쓸 거야"라고 뒤늦게 언급. 그 후 재사용 판정 재조정.
- **배운 점**: 파악 단계에서 "무엇을 재사용/폐기할지" 판정할 때, 사용자에게 폐기 후보를 직접 물어보는 게 낫다. 특히 큰 외부 서비스 종속성(Gemini, Resend 등)은 필히 확인.
- **앞으로의 적용**: 원본 앱을 인수하는 초기 파악 시 "이 앱의 어떤 기능이 필요 없나요?"를 명시적으로 질문해서 판정에 반영.

---

## L-009. 폴더명 오타는 만들 때 잡아야 한다
- **날짜**: 2026-09-03
- **상황**: 로컬 폴더명이 `the-N-magnagement-system` (magnagement 오타).
- **발견/문제**: 세션 중에 폴더명 변경 불가 (Claude Code 세션이 그 폴더에서 실행 중). 세션 종료 후 수동 rename 필요.
- **배운 점**: 프로젝트 폴더 생성 시 이름 한 번 더 확인.
- **앞으로의 적용**: 새 프로젝트 폴더 만들 때 이름 spell check + 하이픈/언더스코어 일관성 확인.

---

## L-010. 원작자의 `parkch-meca/project-management-app` 저장소가 사라졌거나 private일 수 있다
- **날짜**: 2026-09-03
- **상황**: 원본 저장소를 복원 참조하려고 `gh repo view parkch-meca/project-management-app` 실행.
- **발견/문제**: `Could not resolve to a Repository` — public이 아니거나 삭제/renamed됨. 결국 로컬 dangling commit에서 복원.
- **배운 점**: 외부 원본 저장소는 언제든 사라질 수 있다. 참조가 필요하면 로컬 사본을 안전한 위치에 유지.
- **앞으로의 적용**: 프로젝트 인수 초기에 원본 저장소를 별도 보관 폴더에 클론해두기 (예: `~/references/project-management-app/`).

---

## L-011. 스키마가 있다고 가정하기 전에 반드시 확인
- **날짜**: 2026-09-03
- **상황**: Step 3 실행. `hotel_003_tasks_rebuild.sql`은 기존 tasks 테이블에 ALTER를 가하는 방식으로 작성. 실행하니 "tasks 테이블이 없다"는 에러.
- **발견/문제**: 사용자가 새로 만든 Supabase 프로젝트는 완전히 비어있었음. 나는 "이전에 봤던 rnd-task-manager 프로젝트의 R&D 스키마가 이 새 프로젝트에도 있다"고 잘못 가정. 실제로 스키마는 이전 프로젝트에만 있었고, 새 프로젝트는 auth 스키마만 갖춘 상태.
- **배운 점**: 다른 환경으로 마이그레이션 SQL을 옮길 때, "무엇이 이미 있는지"를 먼저 확인해야 한다. `SELECT tablename FROM pg_tables WHERE schemaname='public';` 같은 진단 쿼리로 상태를 봤어야 함.
- **앞으로의 적용**: 스키마 마이그레이션은 두 가지 상태를 모두 지원해야 함 — 기존 스키마 위에 ALTER (upgrade path) vs 빈 DB에 CREATE (fresh install). 최소 CREATE 버전은 항상 준비.

## L-012. Supabase SQL Editor의 auth 컨텍스트는 NULL
- **날짜**: 2026-09-03
- **상황**: `is_superadmin(auth.uid())` 같은 RLS 헬퍼 함수를 SQL Editor에서 테스트하면 항상 false 반환.
- **발견/문제**: SQL Editor는 `postgres` 역할로 실행되어 `auth.uid()`가 NULL. 함수가 NULL 인자를 받아 false. 실제 앱(authenticated user)에서는 정상 동작.
- **배운 점**: RLS 함수 검증은 SQL Editor에서 `auth.uid()` 없이 명시적 UUID로 하거나, 앱에서 직접 호출해야 함.
- **앞으로의 적용**: RLS 헬퍼는 반드시 명시적 파라미터를 받게 작성 (예: `is_superadmin(uid UUID)`). 그리고 `prevent_privilege_escalation` 같은 트리거는 `auth.uid()=NULL`인 경우 (SQL Editor / service role) 통과시켜서 부트스트랩·긴급 복구 가능하게.

## L-013. 초기 프로필 트리거는 사용자 생성 시점에 이미 있어야 한다
- **날짜**: 2026-09-03
- **상황**: `handle_new_user()` 트리거가 있는데도 `auth.users`에는 계정이 있고 `public.profiles`에는 대응 행이 없는 상태 발생.
- **발견/문제**: 사용자가 auth.users에 등록된 시점에 트리거가 아직 없었음. 트리거는 이후에 CREATE 되어도, 이미 생성된 사용자에 대해서는 소급 적용 안 됨.
- **배운 점**: 데이터베이스 초기화 순서: **함수/트리거 먼저 → 사용자 계정 나중**. 반대 순서면 수동으로 backfill 필요.
- **앞으로의 적용**: 초기 셋업 시 반드시 함수/트리거 먼저 설치. 이미 있는 auth.users에 대해서는 다음 backfill 실행:
  ```sql
  INSERT INTO public.profiles (id, email, full_name, role, is_admin)
  SELECT id, email, split_part(email, '@', 1), 'staff', false
  FROM auth.users
  WHERE id NOT IN (SELECT id FROM public.profiles);
  ```

## L-014. 여러 축으로 나뉜 권한은 명시적 헬퍼로 표현
- **날짜**: 2026-09-03
- **상황**: 초기 `is_admin()` 함수가 `is_admin=true OR role IN ('ceo','manager')`로 되어 있어, "시스템 관리자"와 "도메인 관리급"이 섞임. 사용자가 Admin과 CEO/Manager 분리를 원함.
- **발견/문제**: 하나의 함수가 두 가지 뜻을 가지면, RLS 정책 재배치 시 어떤 정책에 어떤 뜻을 적용할지 헷갈림. 특히 나중에 요구사항이 세분화되면 (예: "CEO는 다이제스트 설정 조회만 가능") 정책 재작성이 힘듦.
- **배운 점**: 여러 축의 권한 (시스템 축 vs 도메인 축)은 별도의 헬퍼 함수로 분리해서 표현. `is_superadmin`, `is_ceo_or_above`, `is_management` 처럼 각각 다른 축·수준을 명확히 이름 짓기.
- **앞으로의 적용**: RLS 정책은 헬퍼 함수 이름만 보고 의도를 읽을 수 있어야 함. `is_admin` 같은 애매한 이름 지양.

## L-015. Flutter run은 background 실행 불가
- **날짜**: 2026-09-03
- **상황**: `flutter run -d chrome`을 background로 돌리려 시도.
- **발견/문제**: Flutter run은 interactive TTY(hot reload 키 입력 등)를 필요로 해서 background에서는 pub get만 하고 즉시 종료됨. 서버가 안 뜸.
- **배운 점**: 로컬 dev 서버 실행은 반드시 사용자가 자기 터미널에서 직접. Claude가 background로 못 띄움.
- **앞으로의 적용**: 사용자가 앱을 실행해서 눈으로 확인해야 할 때는 "터미널에서 이 명령어 실행하세요"라고 안내. 배포된 URL이 있으면 그것을 사용.

